// Re-export QUERY_KEYS from constants so components can import from
// '@/utils/queryKeys' per Phase 2 convention. The single source of truth
// remains constants.js.
export { QUERY_KEYS } from '@/utils/constants';

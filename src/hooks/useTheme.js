// Re-export from context so components can import from '@/hooks/useTheme'
// per Phase 2 convention. The actual implementation lives in ThemeContext.
export { useTheme } from '@/context/ThemeContext';
